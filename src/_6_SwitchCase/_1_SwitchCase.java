package _6_SwitchCase;
import java.util.Locale;
import java.util.Scanner;
public class _1_SwitchCase {
    public static void main(String[] args) {


        //Оператор switch – проверяет переменную на равенство
        // в отношении списка значений. Каждое значение
        // называется case, и переменная переключаясь
        // проверяется для каждого case.

       /* Переменные, которые используются в операторе switch,
        могут быть только целые числа, конвертированные в
        целые числа (byte, short, char), строки и перечисления.

        Вы можете иметь любое количество операторов case в
        рамках одного switch. За каждым case следует сравниваемое
        значение, а затем идёт двоеточие.

        Значение case должен быть того же типа данных как и
        переменная в switch, и она должна быть константой или литерал.
        Когда переменная switch на равна оператору case, операторы
        следующие за case будут выполняться до тех пор, пока не будет
        достигнут оператор break.

        При достижении оператора break, switch завершается,
        и поток управления переходит к следующей строке после
        оператора switch.

        Не каждый case должен содержать break.
        Если отсутствует break, поток управления попадет
        на следующие case, до тех пор пока break не будет достигнут.

        В Java оператор switch может иметь дополнительный default case,
        который должен находиться в конце switch. Default case может
        быть использован для выполнения задачи, когда ни один из
        case является правильным. Break не требуется в default case.

        */
        /* int num = 9;


        switch (num){
            case 1:
                System.out.println("Winter");
                break;
            case 2:
                System.out.println("Spring");
                break;
            case 3:
                System.out.println("Summer");
                break;
            case 4:
                System.out.println("Fall");
                break;
            default:
                System.out.println("NOT");*/


// домашняя задание - nested switch


    }
}

